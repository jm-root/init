module.exports = {
  modules: {
    conf: {
      config: require('./conf')
    },
    passport: {
      config: require('./passport')
    },
    acl: {
      config: require('./acl')
    },
    bank: {
      config: require('./bank')
    }
  }
}
